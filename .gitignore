node_modules/
*.log
*.tsbuildinfo

# Local env / token files
.env
.env.*
!.env.example

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Runtime
/tmp/
.cache/

# Android build artifacts (regenerated by `npx cap sync` + gradlew)
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/build/
android/local.properties
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/app/src/main/assets/cordova.js
android/app/src/main/assets/cordova_plugins.js

# Optional: ignore lockfile if you don't want to commit it
# package-lock.json
static/dist/

# Cloned third-party reference repos (design inspiration only, not part of
# agentphone). The curated comparison lives in dev/REPOS.md (also gitignored).
reference/

# Internal dev docs + scratch (not part of the shipped product — STATE/DESIGN/UX-BACKLOG/COMPAT/FEISHU live here)
dev/
